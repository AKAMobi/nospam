qmail need a patch to let smtpd pass child's stderr to smtp client
support SSL/TLS on port 25

qmail /var/qmail directory, should has no control directory, for this directory is in *root* direcotory.
/var/qmail/service/* also  in root/services/*
